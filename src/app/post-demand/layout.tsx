import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Post Demand — FreshHaul',
};

export default function PostDemandLayout({ children }: { children: React.ReactNode }) {
  return children;
}
